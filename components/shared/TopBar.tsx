// Deprecated. The TopBar is now part of <AppShell> — it was inlined so the
// hamburger button could share the menu state with <MobileDrawer>. If you
// need to customise the header for a specific layout, edit AppShell or
// extract a TopBar slot back out of it.
//
// This file is kept as a tombstone so any stale import surfaces immediately.
export {};

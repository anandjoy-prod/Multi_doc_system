// Deprecated. Roles, users, sample sessions, and analytics now come from
// Supabase. The seed values previously lived here are duplicated inline
// in `scripts/seed.mjs` so the seeder is a single self-contained file.
//
// UI-only constants (e.g. STARTER_SUGGESTIONS) moved to `lib/constants.ts`.
//
// Tombstone — kept so any stale import surfaces immediately.
export {};
